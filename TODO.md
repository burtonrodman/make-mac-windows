- Ctrl+S
- ~~Alt+F4 - close current window~~ (shipped as Command+F4 — see `WindowCloser.swift`)
- ~~snap zones~~ (shipped — 1-3 configurable zones per monitor, editable in the
  "Snap Zones…" window; Start+Left/Right cycles the focused window through
  every zone on every monitor. See `SnapZones.swift`/`SnapZonesWindowController.swift`.)